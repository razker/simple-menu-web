import React from 'react';

const Tagline = () => {
    return(
        <section id="lock">
            <h2>SERVING MORE THAN JUST DELICIOUS FOOD</h2>
            <p>Just pick a restaurant and look at the menu.</p>
        </section>
    );
};

export {Tagline};